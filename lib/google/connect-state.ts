/**
 * Name of the short-lived cookie that ties a Google consent redirect to the request that
 * started it. Lives outside the "use server" action file, which may only export functions.
 */
export const CONNECT_STATE_COOKIE = "cf_connect_state";
